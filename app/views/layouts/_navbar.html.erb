<header class="main_menu">
        <div class="container">
            <div class="row align-items-center container-fluid">
                <div class="col-lg-12 col-md-12 col-sm-12">
                    <nav class="navbar navbar-expand-lg">
                      <i class="bi bi-boxes logic-icon text-light" style="font-size: 30px; margin-right:5px"></i>
                        <%= link_to "Ónix", home_path, class:'navbar-brand text-light ml-2 logic-text', style:"font-size: 28px;"%>
                            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarScroll" aria-controls="navbarScroll" aria-expanded="false" aria-label="Toggle navigation">
                              <span class="navbar-toggler-icon"></span>
                            </button>
                        <div class="col-1 col-md-1 col-sm-1 text-center collapse navbar-collapse justify-content-center text-center" id="navbarScroll">
                                  <ul class="navbar-nav me-auto my-2 my-lg-0 navbar-nav-scroll" style="--bs-scroll-height: 100px;">
                                      <% if user_signed_in? %>
                                        <li class="nav-item">
                                          <%= link_to "Home", root_path, class:'nav-link' %>
                                        </li>
                                        <li class="nav-item">
                                          <%= link_to "Evidences", evidences_path	, class:'nav-link' %>
                                        </li>
                                      <% end %>
                                  </ul>
                                  <form class="d-flex">
                                    <%# <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search"> %>
                                    <% if user_signed_in? %>
                                      <%# <%= button_to "Sign out", destroy_user_session_path, method: :delete %>
                                        <li class="nav-item">
                                          <p>Welcome!</p>
                                        </li>
                                      <%= link_to "Sign up", new_user_registration_path, class:'btn btn-outline-warning'  %>
                                      <%= button_to "Sign out", destroy_user_session_path, method: :delete  %>
                                    <% else %>
                                      <%= link_to "Login", new_user_session_path, class:'btn btn-outline-light btn_1 home_page_btn'  %>
                                    <% end %>
                                  </form>
                        </div>
                    </nav>
                </div>
            </div>
        </div>
</header>