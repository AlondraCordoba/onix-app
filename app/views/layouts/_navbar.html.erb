<nav class="navbar navbar-expand-lg navbar-dark bg-dark static mb-5">
  <div class="container-fluid">
  <i class="bi bi-boxes" style="font-size: 1.5rem; color: white"></i>
    <%= link_to "Rails App", root_path, class:'navbar-brand' %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarScroll" aria-controls="navbarScroll" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-center text-center" id="navbarScroll">
      <ul class="navbar-nav me-auto my-2 my-lg-0 navbar-nav-scroll" style="--bs-scroll-height: 100px;">
          <% if user_signed_in? %>
            <li class="nav-item">
              <%= link_to "Home", root_path, class:'nav-link' %>
            </li>
            <li class="nav-item">
              <%= link_to "Evidences", evidences_path	, class:'nav-link' %>
            </li>
          <% end %>
      </ul>
      <form class="d-flex">
        <%# <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search"> %>
        <% if user_signed_in? %>
          <%# <%= button_to "Sign out", destroy_user_session_path, method: :delete %>
            <li class="nav-item">
              <p>Welcome!</p>
            </li>
          <%= link_to "Sign up", new_user_registration_path, class:'btn btn-outline-warning'  %>
          <%= link_to "Log ot", destroy_user_session_path, class:'btn btn-outline-warning', method: :delete  %>
        <% else %>
          <%= link_to "Login", new_user_session_path, class:'btn btn-outline-light'  %>
        <% end %>
      </form>
    </div>
  </div>
</nav>